# dependencies
node_modules/

# nuxt / nitro build outputs
.nuxt/
.output/
dist/
.build/

# exe packaging runtime cache
pack/sea/runtime/

# runtime user data & dev caches
# 注意：写成 `/data/`（仅根目录），早前的 `data/` 会连 `src/data/` 一起忽略
/data/
.cache/
*.log
npm-debug.log*

# dev docs & reverse-engineering materials (kept in dev copy only)
DEVELOPMENT.md
ERROR.md
HANDOVER.md
_mp-analyze/
# 第三方同目标项目研究材料（含厂商版权解包源码）——仅本地留档，绝不入库/分发
Totoro-main*
capture/
.mp-test-build/
*.local

# ⚠️ 隐私红线：抓包存档 / 证书 / 令牌 / 会话一律不得入库（含个人 token、学号、轨迹）
# 见 _mp-analyze/解决方案与取证方案.md 「隐私纪律」
*.saz
*.har
*.pcap
*.pcapng
*.chls*
**/capture/**
*.pem
*.key
*.p12
*.pfx
*.cer
*.crt
*.csr
token*.json
*token*.txt
mp_session*
session*.json
secrets/
private/

# editor
.vscode/
.idea/
.DS_Store
